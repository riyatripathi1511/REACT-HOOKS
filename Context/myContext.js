import { createContext } from "react"

// create context ->

// const myContext = createContext("light");

const myContext = createContext();
// const myIncrease = createContext(0);

export default myContext;